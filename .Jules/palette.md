## 2026-04-21 - Adding accessibility attributes to TaskWidget\n**Learning:** Added aria-label to input field and icon-only buttons (Add Task, Toggle Completion, Delete Task) in TaskWidget.tsx. Dynamically updating aria-labels based on task completion state (e.g., 'Mark as complete' vs 'Mark as incomplete') improves screen reader context.\n**Action:** Always check icon-only buttons and forms for missing aria-labels to ensure keyboard navigation and screen reader accessibility, and use dynamic labels where state changes affect the action's intent.
